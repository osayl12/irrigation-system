# Copy to .env and adjust for your machine:  cp .env.example .env

# HTTP server port
PORT=3000

# MySQL connection (see sql/irrigation_system.sql)
HOST=localhost
USER_DB=root
PASSWORD_DB=
DATABASE=irrigation_system

# MQTT broker (matches mosquitto.conf)
MQTT_URL=mqtt://localhost:1883
